<Window x:Class="WordVCS.UI.Controls.BranchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="分支管理" Width="400" Height="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F9FA">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🌿 分支管理" FontSize="18"
                   FontWeight="Bold" Foreground="#212529" Margin="0,0,0,15" />

        <!-- Create new branch -->
        <Border Grid.Row="1" Background="White" CornerRadius="6"
                BorderBrush="#E9ECEF" BorderThickness="1"
                Padding="12" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="新建分支" FontSize="12" FontWeight="SemiBold"
                           Foreground="#495057" Margin="0,0,0,8" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="NewBranchName" Grid.Column="0"
                             Height="30" FontSize="12" Padding="8,5"
                             VerticalContentAlignment="Center" />
                    <Button Grid.Column="1" Content="创建并切换"
                            Style="{StaticResource ModernButton}"
                            Width="90" Height="30" FontSize="11"
                            Margin="10,0,0,0" Click="OnCreateBranch" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Branch list -->
        <Border Grid.Row="2" Background="White" CornerRadius="6"
                BorderBrush="#E9ECEF" BorderThickness="1"
                Padding="12">
            <StackPanel>
                <TextBlock Text="本地分支" FontSize="12" FontWeight="SemiBold"
                           Foreground="#495057" Margin="0,0,0,8" />
                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Branches}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8,6" Margin="0,0,0,3"
                                        CornerRadius="4"
                                        Background="#F8F9FA"
                                        Cursor="Hand"
                                        MouseLeftButtonDown="OnBranchClicked">
                                    <TextBlock FontSize="12" Foreground="#212529">
                                        <Run Text="{Binding DisplayName}" />
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="关闭" Width="80" Height="32"
                    Background="#E9ECEF" Foreground="#495057"
                    BorderThickness="0" FontSize="13" Cursor="Hand"
                    Click="OnClose" />
        </StackPanel>
    </Grid>
</Window>
